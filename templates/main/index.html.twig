{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block navbar %}
{% endblock %}
       
{% block body %}
<div class="container">
    <div class="jumbotron pt-4" id="homepage-presentation">
		<h1 class="display-4 alt-font">Live Beer Feed</h1>
		<p class="lead">Get live information, tap lists and statistics for your favorite beer events. 
		<br />Receive notifications from the organizers and brewers.
		<br />This service is currently work in progress but feel free to contact me if you would like to include your event!
		<br />Contact: <a href="mailto:robin@livebeerfeed.com">robin@livebeerfeed.com</a></p>
		<hr class="my-2">
		<h1 class="display-5 alt-font">Current events</h3>
        {% if currentEvents|length > 0 %}
			<div class="clearfix">
		    {% for event in currentEvents %}
                {% include 'main/templates/current_event_card.template.html.twig' with { 'event': event } %}
            {% endfor %}
            </div>
        {% else %}
        	<p>We aren't tracking any events at the moment.</p>
        {% endif %}
        {% if upcomingEvents|length > 0 %}
    		<hr class="my-4">
    		<h1 class="display-5 alt-font">Upcoming events</h3>
			<div class="clearfix">
		    {% for event in upcomingEvents %}
                {% include 'main/templates/event_card.template.html.twig' with { 'event': event  } %}
            {% endfor %}
        	</div>
        {% endif %}
        {% if previousEvents|length > 0 %}
			<hr class="my-4">
			<h1 class="display-5 alt-font">Previous events</h1>
			<div class="clearfix">
		    {% for event in previousEvents %}
                {% include 'main/templates/event_card.template.html.twig' with { 'event': event  } %}
            {% endfor %}
    		</div>
        {% endif %}
    </div>
</div>
{% endblock %}
